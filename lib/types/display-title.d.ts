/** Remove catalog boilerplate from the title shown to users without changing the source description. */
export declare function displayTitle(value: string): string;
export declare function githubRepoLabel(repo: string): string;
